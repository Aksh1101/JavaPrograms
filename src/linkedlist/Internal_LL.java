package linkedlist;

import java.util.LinkedList;

public class Internal_LL {
    public static void main(String[] args) {
        LinkedList<Integer> list = new LinkedList<>();
        list.add(1);
        list.add(2);
        list.add(3);

        // this is the linkedlist is provided by the java internally
        // with all the applicable function accessed by list.
    }
}
